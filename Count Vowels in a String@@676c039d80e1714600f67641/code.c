#include <stdio.h>

int main()
{
    char s[100];
    int ctr=0,l;
    printf("");
    scanf("%[^/n]",s)
    for(l = 0;a[l] != '\0'; l++)
    {
        if (a[l] == 'a' || a[l] == 'e' || a[l] == 'i' || a[l] == 'o' || a[l] == 'u' || a[l] == 'A' || a[l] == 'E' || a[l] == 'I' || a[l] == 'O' || a[l] == 'U')
        {
            ctr++;
        }
            

    }
    printf("%d",ctr);
    return 0;
}