#include <stdio.h>

int main()
{
    char str[100];
    int ctr=1,l;
    scanf("%[^\n]",&a);
    for(l = 0;a[l] != '\0'; l++)
    {
        if (str[i] == ' ' && str[i + 1] != ' ' && str[i + 1] != '\n' && str[i + 1] != '\0')
        {
            ctr++;
        }
    }
    printf("%d\n", ctr);
    return 0;
}